# QA Inventory

1. The home screen has one abstract Signal Garden hero with subtle signal nodes and grid texture.
2. Decorative chrome uses `pointer-events: none`.
3. Native sidebar, project selector, cards, task content, and composer remain interactive.
4. Reduced-motion systems use the static PNG fallback.
5. CDP listens only on `127.0.0.1`.
6. Restore removes injected DOM/CSS without changing the official application bundle or `app.asar`.
7. No TTFlows account, API key, Base URL, proxy, or provider configuration is written automatically.